{
  "name": "nexiums",
  "version": "1.0.0",
  "description": "AI-Powered Artifacts & Code Execution Platform",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "executor",
    "shared"
  ],
  "scripts": {
    "install:all": "npm install && npm install --workspace=frontend && npm install --workspace=backend && npm install --workspace=executor && npm install --workspace=shared",
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=executor\" \"npm run dev --workspace=frontend\"",
    "build:all": "npm run build --workspace=shared && npm run build --workspace=backend && npm run build --workspace=executor && npm run build --workspace=frontend",
    "start": "concurrently \"npm run start --workspace=backend\" \"npm run start --workspace=executor\" \"npm run start --workspace=frontend\"",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "clean": "rm -rf node_modules frontend/node_modules backend/node_modules executor/node_modules shared/node_modules",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:build": "docker-compose build",
    "db:migrate": "npm run migrate --workspace=backend",
    "db:seed": "npm run seed --workspace=backend",
    "db:studio": "npm run studio --workspace=backend"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "concurrently": "^8.2.2",
    "prettier": "^3.1.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=9.0.0"
  }
}
